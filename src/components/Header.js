import React from "react";

const Header = () => {
  return (
    <header style={styles.header}>
      <h1>Dream team💫</h1>
    </header>
  );
};

const styles = {
  header: {
    padding: "1em",
    color: "#FFFFFF",
    backgroundColor: "#75163F",
    textAlign: "center",
    fontWeight: "bold",
    marginBottom: "40px",
  },
};

export default Header;
